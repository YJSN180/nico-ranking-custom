# カスタムタグランキング機能 実装計画書

## 1. 概要

### 1.1 機能概要
ユーザーがタグの組み合わせ（AND/OR/NOT条件）でカスタムランキングを作成・保存・閲覧できる機能を実装する。

### 1.2 主要機能
- タグ検索（AND/OR/NOT条件）によるランキングフィルタリング
- カスタムランキングの保存・管理
- 保存したランキング一覧の表示（グリッド形式）
- 既存のランキング表示UIを活用した結果表示

## 2. 画面構成

### 2.1 画面遷移図
```
ホーム
  ↓
カスタムタグ（新規メニュー項目）
  ├─→ カスタムランキング一覧（デフォルト）
  │     ├─→ 新規作成ボタン → タグ検索画面
  │     └─→ 保存済みランキング選択 → ランキング表示画面
  └─→ タグ検索画面
        └─→ 検索結果 → ランキング表示画面
```

### 2.2 画面設計

#### 2.2.1 カスタムランキング一覧画面

**デスクトップ版レイアウト（グリッド形式・最大5列）**
```
┌─────────────────────────────────────────────────────────┐
│ ヘッダー                                                  │
├─────────────────────────────────────────────────────────┤
│ [← 戻る] カスタムタグランキング                           │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ [+ 新規作成]                                            │
│                                                         │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐│
│ │ CARD 1  │ │ CARD 2  │ │ CARD 3  │ │ CARD 4  │ │ CARD 5  ││
│ │         │ │         │ │         │ │         │ │         ││
│ │ タイトル │ │ タイトル │ │ タイトル │ │ タイトル │ │ タイトル ││
│ │ タグ条件 │ │ タグ条件 │ │ タグ条件 │ │ タグ条件 │ │ タグ条件 ││
│ │ 更新日時 │ │ 更新日時 │ │ 更新日時 │ │ 更新日時 │ │ 更新日時 ││
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘│
│                                                         │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐                   │
│ │ CARD 6  │ │ CARD 7  │ │ CARD 8  │                   │
│ └─────────┘ └─────────┘ └─────────┘                   │
└─────────────────────────────────────────────────────────┘
```

**モバイル版レイアウト（縦スクロール・2列）**
```
┌─────────────────────┐
│ ヘッダー             │
├─────────────────────┤
│ [←] カスタムタグ     │
├─────────────────────┤
│ [+ 新規作成]        │
├─────────────────────┤
│ ┌────────┐ ┌────────┐│
│ │ CARD 1 │ │ CARD 2 ││
│ └────────┘ └────────┘│
│ ┌────────┐ ┌────────┐│
│ │ CARD 3 │ │ CARD 4 ││
│ └────────┘ └────────┘│
└─────────────────────┘
```

**カードコンポーネントデザイン**
```
┌──────────────────────────┐
│ 🏷️ ランキング名          │
├──────────────────────────┤
│ タグ条件:                │
│ ✓ タグA AND タグB        │
│ ✓ NOT タグC              │
│                          │
│ ジャンル: すべて         │
│ 期間: 24時間             │
│                          │
│ 📅 更新: 2025/07/24      │
├──────────────────────────┤
│ [編集] [削除]            │
└──────────────────────────┘
```

#### 2.2.2 タグ検索・編集画面

```
┌─────────────────────────────────────────────────────────┐
│ カスタムランキング作成/編集                              │
├─────────────────────────────────────────────────────────┤
│ ランキング名: [___________________________]            │
│                                                         │
│ ベースランキング:                                       │
│ ジャンル: [すべて▼] 期間: [24時間▼]                   │
│                                                         │
│ タグ条件:                                               │
│ ┌─────────────────────────────────────────────────┐   │
│ │ 条件1: [AND▼] [タグを入力...        ] [+ 追加]  │   │
│ │                                                   │   │
│ │ 現在の条件:                                       │   │
│ │ ✓ ゲーム [×]                                     │   │
│ │ ✓ 実況プレイ動画 [×]                            │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ ┌─────────────────────────────────────────────────┐   │
│ │ 条件2: [NOT▼] [タグを入力...        ] [+ 追加]  │   │
│ │                                                   │   │
│ │ 現在の条件:                                       │   │
│ │ ✓ ホラー [×]                                     │   │
│ └─────────────────────────────────────────────────┘   │
│                                                         │
│ [+ 条件グループを追加]                                  │
│                                                         │
│ 人気のタグ（クリックで追加）:                          │
│ [ゲーム] [実況プレイ動画] [VOICEROID実況]              │
│ [ゆっくり実況プレイ] [音楽] [VOCALOID]                 │
│                                                         │
│ [プレビュー] [保存] [キャンセル]                       │
└─────────────────────────────────────────────────────────┘
```

#### 2.2.3 ランキング表示画面（既存のランキング表示を流用）

```
┌─────────────────────────────────────────────────────────┐
│ カスタムランキング: 「ゲーム実況（ホラー以外）」        │
│ 条件: ゲーム AND 実況プレイ動画 NOT ホラー              │
├─────────────────────────────────────────────────────────┤
│ タグ表示: [ON/OFF]                                      │
├─────────────────────────────────────────────────────────┤
│ （以下、既存のランキング表示UIと同じ）                  │
│                                                         │
│ 1位 🥇 [サムネイル] タイトル...                        │
│ 2位 🥈 [サムネイル] タイトル...                        │
│ 3位 🥉 [サムネイル] タイトル...                        │
│ ...                                                     │
└─────────────────────────────────────────────────────────┘
```

## 3. データ構造設計

### 3.1 カスタムランキング型定義

```typescript
// types/custom-ranking.ts

export interface TagCondition {
  operator: 'AND' | 'OR' | 'NOT'
  tags: string[]
}

export interface CustomRanking {
  id: string
  name: string
  genre: string
  period: string
  conditions: TagCondition[]
  createdAt: string
  updatedAt: string
}

export interface CustomRankingList {
  rankings: CustomRanking[]
  maxRankings: number // 最大保存数（例：50）
}
```

### 3.2 ローカルストレージ構造

```typescript
// キー: 'custom-tag-rankings'
{
  version: 1,
  rankings: [
    {
      id: "uuid-1",
      name: "ゲーム実況（ホラー以外）",
      genre: "all",
      period: "24h",
      conditions: [
        {
          operator: "AND",
          tags: ["ゲーム", "実況プレイ動画"]
        },
        {
          operator: "NOT",
          tags: ["ホラー"]
        }
      ],
      createdAt: "2025-07-24T10:00:00Z",
      updatedAt: "2025-07-24T10:00:00Z"
    }
  ]
}
```

## 4. コンポーネント設計

### 4.1 新規コンポーネント

```
components/
├── custom-ranking/
│   ├── custom-ranking-card.tsx      # グリッド表示用カード
│   ├── custom-ranking-card.css      # カードスタイル
│   ├── tag-condition-editor.tsx     # タグ条件エディタ
│   ├── tag-condition-editor.css     # エディタスタイル
│   └── tag-search-input.tsx         # タグ検索入力コンポーネント

app/
├── custom-tags/
│   ├── page.tsx                     # カスタムランキング一覧
│   ├── new/
│   │   └── page.tsx                 # 新規作成ページ
│   └── [id]/
│       ├── page.tsx                 # ランキング表示ページ
│       └── edit/
│           └── page.tsx             # 編集ページ
```

### 4.2 既存コンポーネントの拡張

- `navigation.tsx`: カスタムタグメニュー項目を追加
- `tag-selector.tsx`: 複数選択モードを追加

## 5. API設計

### 5.1 タグフィルタリングロジック

```typescript
// lib/custom-tag-filter.ts

export function filterByCustomConditions(
  items: RankingItem[],
  conditions: TagCondition[]
): RankingItem[] {
  return items.filter(item => {
    return conditions.every(condition => {
      const itemTags = item.tags || []
      
      switch (condition.operator) {
        case 'AND':
          // すべてのタグを含む
          return condition.tags.every(tag => 
            itemTags.includes(tag)
          )
        
        case 'OR':
          // いずれかのタグを含む
          return condition.tags.some(tag => 
            itemTags.includes(tag)
          )
        
        case 'NOT':
          // いずれのタグも含まない
          return !condition.tags.some(tag => 
            itemTags.includes(tag)
          )
        
        default:
          return true
      }
    })
  })
}
```

### 5.2 ストレージ操作

```typescript
// lib/storage/custom-rankings.ts

export class CustomRankingStorage {
  private static readonly STORAGE_KEY = 'custom-tag-rankings'
  private static readonly MAX_RANKINGS = 50
  
  static getAll(): CustomRankingList
  static get(id: string): CustomRanking | null
  static save(ranking: CustomRanking): void
  static update(id: string, ranking: Partial<CustomRanking>): void
  static delete(id: string): void
  static export(): string
  static import(data: string): void
}
```

## 6. 実装フェーズ

### Phase 1: 基礎実装（2-3日）
- [ ] データ型定義とストレージクラス実装
- [ ] ナビゲーションメニューへの追加
- [ ] カスタムランキング一覧ページの基本実装

### Phase 2: タグ検索機能（3-4日）
- [ ] タグ条件エディタコンポーネント
- [ ] タグ検索入力コンポーネント
- [ ] フィルタリングロジックの実装
- [ ] プレビュー機能

### Phase 3: UI実装（2-3日）
- [ ] グリッドレイアウト（デスクトップ5列）
- [ ] モバイル対応（2列）
- [ ] カードコンポーネントのスタイリング
- [ ] アニメーションとインタラクション

### Phase 4: 統合・テスト（2日）
- [ ] 既存のランキング表示との統合
- [ ] E2Eテストの作成
- [ ] パフォーマンス最適化
- [ ] エッジケースの処理

## 7. 技術的考慮事項

### 7.1 パフォーマンス
- タグフィルタリングは1000件のデータに対して実行されるため、効率的なアルゴリズムが必要
- 結果のキャッシュ機構を実装（条件のハッシュをキーとして使用）

### 7.2 制限事項
- カスタムランキングの最大保存数: 50件
- タグ条件の最大グループ数: 5グループ
- 1グループあたりの最大タグ数: 10個

### 7.3 将来の拡張性
- サーバーサイドでの保存（ユーザーアカウント連携）
- カスタムランキングの共有機能
- より複雑な条件式（括弧によるグループ化など）

## 8. セキュリティ考慮事項
- XSS対策: タグ名のサニタイゼーション
- ローカルストレージのサイズ制限チェック
- インポート時のデータ検証

## 9. アクセシビリティ
- キーボードナビゲーション対応
- スクリーンリーダー対応（ARIA属性）
- 高コントラストモード対応

## 10. テスト計画
- 単体テスト: フィルタリングロジック、ストレージ操作
- 統合テスト: コンポーネント間の連携
- E2Eテスト: ユーザーフローの検証
- パフォーマンステスト: 1000件のデータでのフィルタリング速度